#include <stdio.h>
int main()
{
    char symbol;
    int num;
    
    printf("ASCII Symbol?\n");
    scanf("%c", &symbol);
    printf("Repeat Count?\n");
    scanf("%d", &num);
    
    for (int i = 0; i < num; i++) {
    printf("%c ", symbol);
    }
}
