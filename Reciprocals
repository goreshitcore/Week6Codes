#include <stdio.h>
int main (void)
{
    int input;

    printf("?\n");
    scanf("%d", &input);

    
    for ( int i = 1; i <= input; i++ ) {
        float reciprocal = 1.0 / i;
        printf("1 / %d is %f\n", i, reciprocal);
    }
    return 0;
}
