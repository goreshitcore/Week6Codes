#include <stdio.h>
int main (void)
{
    char inputWord[30];
    
    printf("Input a word:\n");
    scanf("%s", inputWord);
    
    for(int i = 0; inputWord[i] != '\0'; ++i) {
        printf("%c\n", inputWord[i]);
    }
    return 0;
}
